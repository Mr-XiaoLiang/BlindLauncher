<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/black"
    tools:context=".LauncherActivity">

    <TextView
        android:id="@+id/textView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="36dp"
        android:textColor="#FBFFFF"
        android:textSize="56sp"
        tools:text="@string/app_name" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:layout_margin="5dp"
        android:gravity="center"
        android:text="@string/discover_light"
        android:textColor="#AAA"
        android:textStyle="italic" />

    <com.lollipop.blindlauncher.view.RotaryView
        android:id="@+id/rotaryView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:runwayBackground="@color/rotaryBackground"
        app:runwayColor="@color/rotaryColor"
        app:runwayWidth="16dp" />

</FrameLayout>